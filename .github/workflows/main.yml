name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run sonarsource analysis
      run: |
        sonar-scanner \
          -Dsonar.projectKey=noche-y-niebla-scrapper \
          -Dsonar.organization=open-source-nan140114 \
          -Dsonar.sources=. \
          -Dsonar.host.url=https://sonarcloud.io \
          -Dsonar.login=ad7b27f28a60014e35e9897f35fc1009f3aea9c2
